clc;
mu0 = 4 * %pi * 1e-7;
I1 = 3;
I2 = 3; 
d = linspace(0.1, 2, 100);
F_per_length = (mu0 * I1 * I2) ./ (2 * %pi * d); 
plot(d, F_per_length, "m"); 
xlabel("Distance (m)"); 
ylabel("Force per Unit Length (N/m)"); 
title("Force Between Two Parallel Conductors (Same Current)");
